# Trained Model Artifacts

Place trained model artifacts here for local prediction:

```text
artifacts_min.joblib
artifacts_max.joblib
```

These files are excluded from Git because they can be large and may contain data-derived model state.
